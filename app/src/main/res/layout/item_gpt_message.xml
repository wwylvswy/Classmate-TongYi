<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="start"
    android:padding="8dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/gpt_avatar"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_marginBottom="4dp"
            android:src="@drawable/ic_gpt_avatar" />

        <TextView
            android:id="@+id/gpt_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginLeft="4dp"
            android:text="Qianwen"
            android:textSize="14sp"
            android:textColor="#000000" />
    </LinearLayout>

    <TextView
        android:id="@+id/gpt_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_gpt_message"
        android:padding="10dp"
        android:textColor="#000000"
        android:textSize="14sp" />
    <!-- 按钮操作区域 -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <!-- 复制按钮 -->
        <ImageButton
            android:id="@+id/btn_copy"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:background="@null"
            android:src="@drawable/chatwithgpt_copy"
            android:contentDescription="Copy message" />
    </LinearLayout>
</LinearLayout>
